services:
  - type: web
    name: taskboard-frontend
    env: static
    buildCommand: cd frontend && pnpm install --frozen-lockfile --dev && pnpm run build
    publishPath: frontend/dist
    nodeVersion: 24.14.1
